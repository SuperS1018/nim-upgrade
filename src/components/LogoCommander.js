import React from 'react'

import '../stylesheets/components/Logo.css'

export default ({ inverted }) => (
  <div
    className='Logo Logo--Commander'
    style={{
      backgroundImage: 'url(/images/slack-admin.png)'
    }}
  />
)
